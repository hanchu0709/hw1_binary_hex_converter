name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Complie
        run: g++ *.cpp -o main
      - name: Run
        run: ./main
